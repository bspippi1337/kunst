name: Build BLCKSWAN ART Android app

on:
  push:
    branches: [main]
    paths:
      - "BLCKSWAN_ART_NATIVE_v1.1.0.zip"
      - ".github/workflows/import-upload.yml"
  workflow_dispatch:

permissions:
  contents: read

env:
  GRADLE_OPTS: >-
    -Dorg.gradle.daemon=false
    -Dorg.gradle.workers.max=2
    -Dkotlin.compiler.execution.strategy=in-process

jobs:
  build:
    name: Build native Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install stable Android toolchain
        shell: bash
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null 2>&1 || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;35.0.0"

      - name: Unpack and patch uploaded project
        shell: bash
        run: |
          set -euo pipefail
          test -f BLCKSWAN_ART_NATIVE_v1.1.0.zip
          PROJECT_DIR="$RUNNER_TEMP/blckswan-art/BLCKSWAN_ART_NATIVE"
          rm -rf "$RUNNER_TEMP/blckswan-art"
          mkdir -p "$RUNNER_TEMP/blckswan-art"
          unzip -q BLCKSWAN_ART_NATIVE_v1.1.0.zip -d "$RUNNER_TEMP/blckswan-art"
          test -f "$PROJECT_DIR/settings.gradle.kts"
          sed -i 's/compileSdk = 37/compileSdk = 36/' "$PROJECT_DIR/app/build.gradle.kts"
          sed -i 's/targetSdk = 37/targetSdk = 36/' "$PROJECT_DIR/app/build.gradle.kts"
          sed -i 's/version "9.3.0"/version "8.13.2"/' "$PROJECT_DIR/build.gradle.kts"
          echo "PROJECT_DIR=$PROJECT_DIR" >> "$GITHUB_ENV"

      - name: Validate exhibition archive
        shell: bash
        run: python -m json.tool "$PROJECT_DIR/app/src/main/assets/exhibition.json" >/dev/null

      - name: Build debug APK
        shell: bash
        working-directory: ${{ env.PROJECT_DIR }}
        run: gradle --no-daemon --max-workers=2 --stacktrace clean assembleDebug

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: BLCKSWAN-ART-debug
          path: ${{ env.PROJECT_DIR }}/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
